# ProjectB 开发过程反思

这次项目让我认识到，Superpowers 的价值并不是让模型“写得更多”，而是迫使我持续回答两个问题：究竟要做什么，以及怎样证明做对了。项目最初只有“用 AI 辅助学习”的模糊方向。经过 brainstorming，我逐步确定目标用户、操作系统课程场景，以及材料与来源、理解检查、复习计划三个模块。这个过程避免了项目退化成普通聊天工具，也让我意识到工程深度来自明确的数据流、状态和约束。

对我帮助最大的技能首先是 brainstorming。它不断追问真实痛点、材料类型、隐私边界、模型权限和验收方式，使很多原本隐含的想法变成了可讨论的选择。尤其有效的是可视化策划案。对于“本地解析、按页外发、整份文件外发”这类抽象取舍，只看文字很难判断用户是否能理解；交互 mockup 把保真度、隐私、费用和重新授权放进实际导入流程后，我能直接判断信息层级、桌面与移动端阶段线以及设置入口是否合理。可视化让产品决策更快，也减少了我和 Agent 对同一描述产生不同想象的可能。

但 brainstorming 和 writing-plans 也是前期成本最高的部分。文档打基础花费了过长时间，很多精力用于反复补充字段、哈希、门禁、所有权和证据格式。早期计划一度包含 69 个派发单元、343 个检查项和大量占位符，经过评审后仍因任务过大、路径所有权不清、验收链不闭合而失败。之后又经历范围收束、计划重写和多轮同快照评审。它证明了严格计划确实能提前暴露问题，但也说明“写得更详细”不必然等于“更可执行”。如果文档复杂度接近实现本身，流程就会从风险控制转变为新的风险来源。

TDD 在本项目中总体是放大器，但并非所有场景都同样划算。对于确定性规则、安全扫描器和协议解析，红—绿—重构非常有效。例如凭据扫描器先以 `scanner_missing` 准确失败，最小实现后再通过合同测试；Claude Code 输出协议问题也通过失败样例区分安全提示、合法 JSON 与异常前言。这些测试既防止回归，也迫使合同可观察。不过，对纯文档排版或风险极低的文字修改，如果机械套用完整红绿、双评审和全量验证，形式成本可能超过收益。更合理的做法是保留“行为变化必须先失败”的原则，同时按风险决定验证深度。

subagent-driven 工作流没有让我得到一个可以无限自主运行的智能体。实践表明，自主运行时间不是最重要的指标，边界内完成率才是。过大的 T-01、T-02 计划包含几十个路径和步骤，fresh agent 即使理解目标，也难以在一次会话内可靠闭合。拆分后，较合适的 task 应只有一个可验证目标、清晰的文件所有权、一条预期失败命令和一组完成标准，并能在上下文仍然新鲜时完成。若任务同时修改共享计划、数据库结构、前后端和 CI，它就已经过大。冷启动中还出现过模型返回空产物、网关 504、输出协议不兼容等情况，说明外部智能体、网络和工具环境都不是稳定资源；Superpowers 的繁琐评测虽然提高了置信度，却显著降低了工作效率，还产生了费用和上下文管理成本。

SPEC 和 PLAN 的质量会直接决定 subagent 的偏离程度。一个具体案例是材料来源定位器最初没有明确绑定 `material_version_id`。如果只绑定材料和页码，解析器升级后同一页可能对应不同抽取结果，subagent 就可能覆盖旧版本或让历史学习证据失去可追溯性。评审暴露该缺口后，规约补充了版本、原始内容哈希和抽取合同，使实现选择从“凭理解猜测”变成可以测试的唯一合同。另一个案例是扫描器路径和语法没有在冷启动输入中闭合，陌生智能体在红测前停下提问。这些失败说明，主 Agent 和我共享的隐性上下文不能算规约内容；只有陌生智能体也能仅凭文件继续，计划才真正可执行。

最有效的 prompt/context 策略是减少背景噪声，而不是塞入全部历史。给 subagent 的上下文应包含冻结的 SPEC/PLAN、精确哈希、单一 task、文件边界、红绿命令和遇到歧义即停止的规则。对评审者则明确区分规约符合性与质量安全评审。结构化输入、明确输出和可复现命令比长篇背景更能降低漂移；可视化问题应尽早用 mockup 讨论，技术取舍仍用文本和证据表表达。

凭据和分发要求迫使我提前考虑许多原本容易被忽略的问题。API key 不能只是“放进 `.env`”，还要考虑隐藏录入、Windows Credential Manager、状态查询不回显、更新与清除、日志脱敏、浏览器和数据库不落密，以及提交前失败关闭的扫描。分发也不只是“本机能启动”，而是要面对 Windows 单文件、OCI 演示、干净环境启动、架构限制、CI 平台冲突、镜像与许可证证据、公开 WebUI 的安全边界。这些要求增加了工作量，却把一次性 demo 推向了可复现的软件工程交付。

如果重做，我会先为规约阶段设时间盒，优先确定最小用户闭环和最高风险合同，再逐步补充证据，而不是在实现前穷举所有未来细节。我还会更早建立自动化机械审计，减少人工反复检查编号、依赖和字段；当独立评审连续只产生同类低风险问题时，停止无上限重评，转为记录剩余风险并进入小规模实现验证。可视化策划应保留，因为它明显提升了决策效率；但研究文档、过程日志和当前权威规约应分层，避免活跃文档被历史证据淹没。

我对 Superpowers 的总体评价是：它擅长守住纪律，却默认需求能够被充分写清、工具和会话相对稳定、评审成本较低、任务可以良好切分。这些假设在本项目中只部分成立。Windows 沙箱、插件可用性、网络超时、模型输出差异和共享文件所有权都让流程比理想情况更曲折。因此 Superpowers 不应被当作必须逐字执行的仪式，而应被理解为风险控制框架。真正需要学生承担的工作，是判断哪些约束保护了产品质量，哪些步骤已经形式大于实质，并在保留可追溯证据的同时及时收束范围。对我而言，这正是本项目最重要的收获：AI 可以承担大量组织、生成和检查工作，但工程师仍必须决定目标、权衡成本，并对最终事实负责。

AI 协助声明：本文观点、案例和初稿由学生本人提供，AI 仅协助语句润色、结构整理与字数检查，未代写项目经历或评价结论。
