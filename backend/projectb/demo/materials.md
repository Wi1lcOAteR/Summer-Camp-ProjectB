# Concurrent Systems Demo Notes

A mutex protects a critical section so only one participant enters it at a time.

A race condition occurs when an outcome depends on an uncontrolled ordering of operations.

A deadlock is a persistent wait cycle in which each participant needs a resource held by another.
