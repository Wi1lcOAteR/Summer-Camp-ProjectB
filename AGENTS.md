# AI4SE 期末项目 B：智能体工作约束

本文件适用于 AI4SE 期末项目 B（非 Coding Agent Harness 的应用类项目）。所有编码智能体在开始工作前必须阅读并遵守本文件；若仓库内存在更具体的子目录级 `AGENTS.md`，其规则仅可补充本文件，不得放宽课程硬性要求。

## 1. 项目责任与优先级

1. 最终责任人是学生本人。智能体负责提出方案、执行已批准任务、提供验证证据，不替学生作未经确认的产品决策。
2. 指令优先级：课程原始要求 > 用户当前明确指令 > `SPEC.md` > `PLAN.md` > 本文件 > 代码中的惯例。
3. 若这些来源冲突、关键文件缺失或目标存在多种会显著改变结果的解释，立即暂停并向用户说明，不得自行猜测。
4. 不得替学生代写 `REFLECTION.md`。可在学生提供初稿后校对、压缩或指出论证缺口，并明确记录 AI 辅助范围。
5. 使用第三方代码、模板或资产前核验许可证，并在 README 的第三方依赖/许可章节中记录来源和许可证。

## 2. 开发阶段门禁

### 阶段 A：规约

- 必须调用 Superpowers 的 `brainstorming`。
- 通过逐项提问澄清用户、问题、功能边界、非功能需求、数据模型、安全、凭据、分发、部署和验收标准。
- 将确认后的设计写入 `SPEC.md`。
- `SPEC.md` 至少包含：问题陈述、5 个以上符合 INVEST 的用户故事、按模块划分的功能规约、非功能需求、架构与数据流、数据模型、凭据威胁模型、分发设计、技术选型、逐项验收标准、风险与未决问题。

### 阶段 B：计划

- 仅在用户确认 `SPEC.md` 后调用 `writing-plans` 生成 `PLAN.md`。
- 每个 task 必须写明：编号、目标、依赖、可否并行、涉及文件、实现要点、预期先失败的测试、验证命令、完成标准。
- task 应足够小，使一个新鲜 subagent 能在一次会话内完成；不得用“完成后端”“实现所有页面”一类大任务掩盖拆分不足。

### 阶段 C：陌生智能体冷启动验证

- 在编写正式实现前，必须由不同类型、全新 session、无历史上下文的智能体，仅凭 `SPEC.md` 与 `PLAN.md` 尝试 1–2 个 task。
- 冷启动智能体遇到不确定处必须暂停提问，不得猜测。
- 将提问、误解、产出差距及修订前后关键 diff 写入 `SPEC_PROCESS.md`。

### 实现门禁

在以下条件全部满足前，禁止编写正式实现代码：

- 用户已确认 `SPEC.md`；
- `PLAN.md` 已完成；
- 陌生智能体冷启动验证已执行；
- 暴露出的规约缺陷已修订；
- 用户已批准进入实现阶段。

## 3. Superpowers 工作流

必须按项目实际阶段使用上游 Superpowers skill：

1. `brainstorming`
2. `writing-plans`
3. `using-git-worktrees`
4. `subagent-driven-development` 或 `executing-plans`
5. `test-driven-development`
6. `requesting-code-review`（并处理 `receiving-code-review`）
7. `finishing-a-development-branch`

同时使用 `verification-before-completion`；遇到故障时先用 `systematic-debugging`。任何无法使用或有理由偏离的步骤，都必须在 `AGENT_LOG.md` 记录原因、替代措施和影响，不得静默跳过。

## 4. Git、worktree 与任务执行

- 每个独立功能或大模块使用独立 worktree 和分支；一个 worktree 对应一个 PR/MR。
- 每个 PLAN task 派发给一个上下文干净的 subagent；任务说明只包含该 task 所需上下文、约束和验证方法。
- 存在共享文件、共享数据库迁移或前置依赖时不得盲目并行；先明确所有权与合并顺序。
- 不得重写用户已有改动，不得运行破坏性 Git 命令，不得强推。
- 不允许一次提交全部项目。commit 应小而可审查，并在 commit message 或 PR/MR 描述中记录 subagent 身份及人工修改。
- 每完成一个 task，立即在 `PLAN.md` 标记状态并附 commit hash；同时更新 `AGENT_LOG.md`。

## 5. TDD 与评审纪律

每个行为变更都按红—绿—重构执行：

1. 先写能表达需求的最小失败测试；
2. 实际运行测试并保存失败证据，确认失败原因正确；
3. 编写使其通过的最少实现；
4. 再次运行并保存通过证据；
5. 在测试保护下重构；
6. 运行相关测试与全量测试。

不得先写实现后补测试，不得删除、跳过或弱化失败测试来制造通过。无法自动测试的 UI 或外部集成，仍须优先测试可分离逻辑，并补充可复现的端到端或人工验收步骤。

每个 task 完成后必须进行两阶段评审：

- 先检查是否符合 `SPEC.md` 与该 task 的验收标准；
- 再检查正确性、可维护性、安全、测试质量和许可证。

Critical issue 修复并复验前不得开始下一 task。

## 6. 应用类项目边界

- 项目至少包含 3 个职责清晰的功能模块，并能在 30 秒内解释真实用户为什么需要它。
- 纯单轮生成、问答或摘要不算 agent，可作为普通模块实现。
- 若某部分具有“自主多轮决策 + 自主调用工具 + 根据反馈自我修正”，它属于 agent。其主循环、工具分发和治理护栏必须自行编码，并在移除真实 LLM 后仍可用 mock/stub 做确定性单元测试；不得仅配置现成 agent 框架的提示词。
- 项目必须提供可访问的 WebUI。若当前方案是纯 CLI/后端，必须在 `SPEC.md` 阶段解决这一交付冲突后才能实现。
- 含任何前端/UI 时使用 Open Design 工作流，并在 `SPEC.md` 记录所选 design system 与 skill；若决定不用，先让用户确认并在 `AGENT_LOG.md` 说明理由。

## 7. 安全与凭据

- API key、token、密码和私钥不得硬编码、提交到 Git、写入日志、测试快照或命令历史。
- 优先使用操作系统钥匙串、密钥管理服务或带主密码的加密文件。`.env` 只能作为已说明明文风险的兼容来源，必须加入 `.gitignore`，并提供不含真实值的 `.env.example`。
- 首次运行须引导用户隐藏录入凭据，并提供查看状态、更新、清除能力；查看状态不得回显明文。
- 外部输入、上传文件、URL、模型输出和工具参数均视为不可信数据，按技术栈执行校验、权限控制、超时、大小限制和错误脱敏。
- 每次提交前执行凭据扫描；发现疑似真实凭据时立即停止，报告文件和风险，不得在输出中复述完整值。

## 8. 测试、CI、分发与部署

- 提供一条可一键执行的测试命令，如 `make test` 或技术栈等价命令。
- 核心功能必须有自动化测试；CI 每次 push 自动运行。
- 必须提供 `.gitlab-ci.yml`，且包含名称严格为 `unit-test` 的 job；最后一次课程提交所对应的 CI/CD 必须通过。
- 原要求同时提及 GitHub PR/Actions 与 NJU Git/GitLab CI，存在平台表述冲突。默认保守实现为：NJU Git + GitLab CI 满足硬性提交项；若维护 GitHub 仓库/镜像，再补 `.github/workflows/ci.yml` 和 PR 记录。不要静默删除任一平台证据，最终策略须由用户确认并记录。
- 分发至少选择容器、原生二进制或包管理器之一。README 必须写清获取、运行、安全配置 key、平台/架构/依赖限制。
- 若选择容器，验证干净环境下单条 `docker build` 与单条 `docker run` 可启动；CI 构建镜像。
- 必须提供截止前可访问的 WebUI URL，并在 README 记录部署架构和 CI/CD。

## 9. 文档与过程证据

必须维护：

- `SPEC.md`
- `PLAN.md`
- `SPEC_PROCESS.md`
- `AGENT_LOG.md`
- `README.md`
- `REFLECTION.md`（仅学生本人撰写，AI 只可按声明范围辅助润色）

`AGENT_LOG.md` 每条记录包含：时间戳、task 编号、触发的 Superpowers skill、关键 prompt/context、subagent 输出或 commit hash、人工修改及原因、经验教训。

README 必须明确包含：项目简介、安装、运行、分发命令、目录结构、安全边界、凭据配置、已知限制、第三方依赖与许可证。

禁止事后伪造对话、失败测试、时间戳、人工干预或 CI 证据。没有证据时明确写“尚未执行”。

## 10. 完成定义

任何 task 或项目只有在下列条件满足时才可声称完成：

- 对应验收标准逐项满足；
- 已展示本次运行产生的测试、构建、静态检查和必要 UI 验证结果；
- 两阶段评审无未解决的 Critical issue；
- `PLAN.md` 与 `AGENT_LOG.md` 已同步更新；
- 不含真实凭据，许可证记录完整；
- 分发产物可在目标环境复现；
- 课程要求的最终 CI 为通过状态。

不得使用“应该能工作”“看起来已通过”代替验证证据。
